class Moodli {

    /**
     * Checks whether to display the meter or not.
     */
    static init(){
        API.send("")
    }

    /**
     * Report today's mood.
     * @param mood A mood on a range of 0 (best) to 2 (worst)
     */
    static report(mood) {

    }
}